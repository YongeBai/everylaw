import { expect, test } from "@playwright/test";
test("mobile layout keeps the front page and voting usable", async ({ page }) => { await page.goto("/"); await expect(page.getByTestId("post-list")).toBeVisible(); const overflow = await page.evaluate(() => document.documentElement.scrollWidth - document.documentElement.clientWidth); expect(overflow).toBeLessThanOrEqual(1); await page.goto("/us/title-18/700"); await expect(page.getByTestId("vote-keep")).toBeInViewport(); await expect(page.getByTestId("vote-dissolve")).toBeInViewport(); await page.waitForLoadState("networkidle"); });
